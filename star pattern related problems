#include <stdio.h>

int main() {
    int n= 4,i,j;

    for(i = 0; i < n; i++) {
        for(j = 1; j < (n*2) - (2 * i); j++) {
            printf("%d", j);
        }
        printf("\n");
    }

    return 0;
}
