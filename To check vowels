#include <stdio.h>
int main()
{
    char str[100];
    scanf("%s",str);
   char s[100]="aeiou";
   int l=0;
   int count=0;
   while(str[l]!='\0'){
     for(int i=0;i<5;i++){
       if(str[l]==s[i]){
         count++;
       }
     }
     l++;
   }
   printf("%d ",count);
    return 0;
}
